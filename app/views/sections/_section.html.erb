<% unless section.new_record? %>
<div id="<%= dom_id(section) %>" class="section clearfix">
  <!-- <%= link_to fa_icon("trash"), method: :delete, remote: true, class: "text-danger section-delete", data: { confirm: "Are you sure you want to delete the section titled \"#{ section.title }\"?"} %>
  <%= link_to fa_icon("pencil"), '#', remote: true, class: "section-edit" %> -->
  <%= link_to section.title, book_chapter_section_path(section.chapter.book, section.chapter, section), class: "section-title", remote: true %>
</div>
<% end %>

<!-- edit_book_chapter_section_path(chapter.book, chapter) -->
<!-- book_chapter_sections_path(chapter.book, chapter) -->
 <!-- remote: true, -->
